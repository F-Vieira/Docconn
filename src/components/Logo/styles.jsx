import styled from "styled-components";

export const Container = styled.h1`
  font-size: 3rem;
  font-family: var(--secundary_font);
  font-weight: bold;
  letter-spacing: 3px;

  color: var(--dark_blue);
`;
