import styled from "styled-components";

export const Container = styled.button`
  width: 100px;
  height: 30px;
  border-radius: 4px;

  background-color: var(--blue);
  color: var(--white);

  font-weight: bold;
  letter-spacing: 1px;
`;
